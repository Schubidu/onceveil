# Adapters

Infrastructure adapters live here.

Planned examples are D1, SQLite, Turnstile, and ALTCHA. They must implement contracts owned by the framework-independent core instead of leaking runtime APIs into it.

No adapter is implemented in issue #1.
